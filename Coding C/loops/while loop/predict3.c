#include<stdio.h>
int main (){
    // int x=4,y,z;
    // y=--x;
    // z=x--;
    int x=4,y=3,z;
    z=--x -y;
    printf("\n%d%d%d",x,y,z);
    return 0;
}
//x++ ->means use existing value of x and then increament 
//++x ->means first do increment of x and then use 