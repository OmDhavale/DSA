#include<stdio.h>
int main (){
    int i=12;
    do {
        printf("''code will run once''\n");
    } while (i<12);
    printf("Then it will check condition..\nin case if it is not satisfied then it will \nkeep quiet then (after executing code once)");
    return 0;
}